import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('betPronos app smoke test', (WidgetTester tester) async {
    // This test is intentionally minimal.
    // Full integration tests require a Supabase instance.
    expect(true, isTrue);
  });
}
